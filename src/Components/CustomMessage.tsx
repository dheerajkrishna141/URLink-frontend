import { Box, Heading, Text } from "@chakra-ui/react";
import React from "react";

const CustomMessage = () => {
  return (
    <Box p={100}>
      <Heading>Go ahead and Add a Few URL's...</Heading>;
    </Box>
  );
};

export default CustomMessage;
